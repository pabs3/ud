## vim: set ft=mako:
% for i, user in enumerate(users):
%     if user.is_not_retired() and user.is_not_guest_account() and user.mailDisableMessage:
${user.uid}: ${user.mailDisableMessage}
%     endif
% endfor
